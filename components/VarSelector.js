import React from "react";
import { View, Text } from "react-native";

function VarSelector({
	n,
	coefficients,
	setCoefficients,
	constants,
	setConstants,
}) {
	return (
		<View>
			<Text>Hello from var-selector</Text>
		</View>
	);
}

export default VarSelector;
